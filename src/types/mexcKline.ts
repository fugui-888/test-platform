/** Same shape as monitor-platform MEXCKLINE (Binance / merged bars). */
export type MEXCKLINE = {
  time: number[];
  open: number[];
  close: number[];
  high: number[];
  low: number[];
  vol: number[];
};
